import { Routes } from '@angular/router';
import { HomeMfeComponent } from './pages/home/home.component';

export const mfeRoutes: Routes = [
    {
        path: '',
        component: HomeMfeComponent,
    },
];
